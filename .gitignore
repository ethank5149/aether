# NOTE: this repository is public. There is deliberately no ignore rule for
# controlled material here — controlled work lives in a separate repository
# (`aether-gambit`), not in an ignored subdirectory of this one. An ignored
# directory is one `git add -f` away from being published; a separate
# repository is not. Do not reintroduce a `controlled/` rule.
#
# The one-way dependency is enforced by `tools/check_boundary.py`.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

manuscripts/*/.latexmkrc

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Results are regenerated by `python -m aether.verification`
results/

# In Progress, Check Back Soon
manuscripts/references/
manuscripts/legacy/
manuscripts/notes.md

# LaTeX build artifacts. Recursive: the manuscript is three papers, each
# built in its own directory (manuscript/paper{1,2,3}/), so per-file rules
# at one level no longer suffice.
manuscripts/**/*.aux
manuscripts/**/*.bbl
manuscripts/**/*.blg
manuscripts/**/*.fdb_latexmk
manuscripts/**/*.fls
manuscripts/**/*.log
manuscripts/**/*.out
manuscripts/**/*.toc
manuscripts/**/*.synctex.gz

# Editor build button writes here (latex-workshop.latex.outDir = %DIR%/build).
manuscripts/**/build/
